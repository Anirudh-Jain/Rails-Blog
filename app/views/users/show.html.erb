<h2> User Profile</h1><br>
<img src = <%= @user.avatar %> />
<h5 class="text-center mt-4"><strong>Name: </strong><%= "#{@user.first_name} #{@user.last_name unless @user.last_name.nil?}" %></h3>
<h5 class="text-center mt-4"><strong>Age: </strong><%= @user.age %></h3>
<h5 class="text-center mt-4"><strong>Email: </strong><%= @user.email %></h3>

<% if !not_logged_in? && @user == current_user %>
    <div class = 'text-center mt-4'><%= link_to 'Edit your profile', edit_user_path(@user), class: "btn btn-outline-info" %></div>
<% end %>

<h3 class="text-center mt-4">Articles</h3>

<%= render 'shared/article' %>